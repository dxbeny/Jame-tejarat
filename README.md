extract 15 number: این کد برای محک کار می کند و تمام سریال های 15 رقمی را در کنار ان نمایش می دهد
#######################################################################################
test&result: این کد برای استفاده از نتایج به دست امده از جامع برای تست های کامل یه دستگاه استفاده میشود. صورتحساب های ردیف ب درنتایج را در فایل اکسل دوم در ردیف ب پیدا میکند و قرمز میکند
hint : just xlsx file
نکته‌های مهم:
هر دو فایل باید باز باشن تا کد کار کنه (اسم دقیق فایل‌ها رو با پسوند، مثل a.xlsx، بذار).
اگه می‌خوای رنگ پس‌زمینه سلول قرمز بشه نه رنگ متن، به‌جای Font.Color از Interior.Color استفاده کن.
اگه شماره‌ها ممکنه با فاصله یا فرمت متفاوت (متن vs عدد) ذخیره شده باشن، بگو تا مقایسه رو دقیق‌تر کنم (مثلاً حذف صفرهای اضافی یا مقایسه به‌صورت عدد).
اگه تعداد ردیف‌ها خیلی زیاده (چند هزارتا) بگو تا نسخه سریع‌تر با Dictionary بنویسم که به‌جای حلقه تو در تو، خیلی سریع‌تر اجرا بشه.
########################################################################################
